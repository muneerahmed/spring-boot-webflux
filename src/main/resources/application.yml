server:
  servlet:
    context-path: /web

logging:
  level:
    com.spring.example: DEBUG
    org.springframework.web: INFO
    reactor.netty.http.client: INFO

spring:
  mvc:
    log-request-details: true
