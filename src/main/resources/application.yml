server:
  servlet:
    context-path: /web

logging:
  level:
    org.springframework.web: INFO
    reactor.netty.http.client: INFO
    com.example: DEBUG

spring:
  mvc:
    log-request-details: true
